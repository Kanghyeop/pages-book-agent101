---
title: "알림 시스템 설정"
nav_order: 3
parent: 부록 A
---


```
claude --permission-mode acceptEdits
```

---

# Claude Notifier

## 🔔 Claude에게 보낼 프롬프트

```
Claude Code에서 네가 나한테 응답을 기다릴 때 자동으로 알림음을 재생하는 설정을 해줘.

## 요구사항

1. 전역 설정 파일 수정
   - 위치: `~/.claude/settings.json` (Windows: `C:\Users\<사용자이름>\.claude\settings.json`)
   - 모든 프로젝트에서 자동으로 작동하도록 전역 설정에 추가

2. Hooks 설정
   - `Notification` 이벤트: Claude가 사용자 응답 대기 시 알림음 재생
   - `Stop` 이벤트: Claude가 작업 중단 시 알림음 재생

3. 알림음 파일
   - Windows: `C:\Windows\Media\Alarm02.wav` (Notification용)
   - Windows: `C:\Windows\Media\Alarm01.wav` (Stop용)
   - PowerShell 명령어로 재생: `powershell -Command "(New-Object System.Media.SoundPlayer '<파일경로>').PlaySync()"`

4. Permissions 설정
   - PowerShell 명령어 실행 권한 추가
   - 최소 권한: `"Bash(powershell:*)"`

## 작동 원리

Claude Code의 Hooks 시스템을 이용:
- Claude가 특정 이벤트 발생 시 (Notification, Stop) 자동으로 hook 실행
- Hook에서 PowerShell 명령어로 Windows 시스템 사운드 재생
- Permissions에 PowerShell 실행 권한이 있어야 작동

## 설정 파일 형식

기존 `~/.claude/settings.json` 파일을 읽어서:
- 이미 hooks나 permissions가 있으면 병합
- 없으면 새로 추가
- 파일이 없으면 새로 생성

## 작업 순서

1. 전역 설정 파일 경로 확인
2. 기존 설정 읽기 (있다면)
3. Hooks와 Permissions 추가/병합
4. 파일 저장
5. 설정 내용 확인 후 나에게 보여주기
6. Claude 재시작 안내

지금 바로 설정해줘.
```

---

## 📋 더 간단한 버전 (복사-붙여넣기)

```
~/.claude/settings.json 파일에 다음 설정을 추가해줘:

1. Hooks:
   - Notification 이벤트 → Alarm02.wav 재생
   - Stop 이벤트 → Alarm01.wav 재생

2. Permissions:
   - Bash(powershell:*) 권한 추가

이유: Claude가 응답 대기할 때 자동으로 알림음 재생하기 위함

기존 설정이 있으면 병합하고, 없으면 새로 만들어줘.
```

---

## 🎯 기술 명세서 버전

### 목적
Claude Code에서 사용자 인터랙션이 필요한 시점에 자동 알림을 제공하여 응답 지연을 최소화

### 구현 요구사항

#### 1. 설정 파일
- 경로: `~/.claude/settings.json` (전역 설정)
- 형식: JSON
- 인코딩: UTF-8

#### 2. Hooks 구성

2.1. Notification Hook
- 트리거: Claude가 사용자 응답 대기 시
- 동작: 시스템 알림음 재생
- 구현:
```json
{
  "hooks": {
    "Notification": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"(New-Object System.Media.SoundPlayer 'C:\\Windows\\Media\\Alarm02.wav').PlaySync()\""
          }
        ]
      }
    ]
  }
}
```

2.2. Stop Hook
- 트리거: Claude 작업 중단 시
- 동작: 시스템 알림음 재생
- 구현:
```json
{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"(New-Object System.Media.SoundPlayer 'C:\\Windows\\Media\\Alarm01.wav').PlaySync()\""
          }
        ]
      }
    ]
  }
}
```

#### 3. Permissions 구성

3.1. 필수 권한
```json
{
  "permissions": {
    "allow": [
      "Bash(powershell:*)"
    ]
  }
}
```

3.2. 확장 권한 (선택사항)
```json
{
  "permissions": {
    "allow": [
      "WebSearch",
      "Bash(powershell -Command \"[System.Media.SystemSounds]::Asterisk.Play\\(\\)\")",
      "Bash(powershell -Command \"[System.Media.SystemSounds]::Exclamation.Play\\(\\)\")",
      "Bash(powershell -Command \"[console]::beep\\(800, 300\\)\")",
      "Bash(powershell:*)"
    ]
  }
}
```

#### 4. 플랫폼별 구현

Windows (PowerShell)
```powershell
powershell -Command "(New-Object System.Media.SoundPlayer '<파일경로>').PlaySync()"
```

Mac (afplay)
```bash
afplay /System/Library/Sounds/Glass.aiff
```

Linux (paplay)
```bash
paplay /usr/share/sounds/freedesktop/stereo/complete.oga
```

### 작동 흐름

```
1. Claude 이벤트 발생 (Notification/Stop)
   ↓
2. settings.json의 hooks 검색
   ↓
3. matcher 확인 (빈 문자열 = 모든 이벤트 매칭)
   ↓
4. command 실행 (permissions 확인)
   ↓
5. 알림음 재생
   ↓
6. 사용자 인지
```

### 구현 작업

1. 설정 파일 확인
   - `~/.claude/settings.json` 존재 여부 확인
   - 없으면 생성, 있으면 기존 내용 읽기

2. JSON 병합
   - 기존 hooks 보존하며 새 hooks 추가
   - 기존 permissions 보존하며 새 permissions 추가

3. 파일 저장
   - JSON 포맷 검증
   - 파일 쓰기

4. 검증
   - 작성된 설정 읽기
   - 사용자에게 결과 표시

5. 안내
   - Claude 재시작 필요 안내
   - 테스트 방법 안내

### 완성된 설정 파일 예시

```json
{
  "hooks": {
    "Notification": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"(New-Object System.Media.SoundPlayer 'C:\\Windows\\Media\\Alarm02.wav').PlaySync()\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"(New-Object System.Media.SoundPlayer 'C:\\Windows\\Media\\Alarm01.wav').PlaySync()\""
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "WebSearch",
      "Bash(powershell:*)"
    ]
  }
}
```

### 성공 기준

- [ ] 설정 파일이 올바른 형식으로 생성됨
- [ ] Claude 재시작 후 Notification 이벤트 시 알림음 재생
- [ ] Claude 재시작 후 Stop 이벤트 시 알림음 재생
- [ ] 기존 설정이 있을 경우 손실 없이 병합됨

---

## 사용 방법

1. 위의 프롬프트 중 하나를 선택
2. Claude Code에 복사-붙여넣기
3. Claude가 자동으로 설정 파일 생성/수정
4. Claude 재시작
5. 완료!

---
<!-- LLM Context Anchor -->
**핵심 요약**: Claude Code 알림 시스템 설정 가이드. 전역 설정 파일: `~/.claude/settings.json`. Hooks 설정: Notification/Stop 이벤트에 알림음 연결. Windows: PowerShell로 시스템 사운드 재생, Mac: afplay. Permissions에 `Bash(powershell:*)` 권한 필요. Auto-accept와 결합하면 다른 작업 하다가 돌아올 시점 알 수 있음.

**키워드**: `알림시스템` `Hooks` `settings.json` `Notification` `Auto-accept`
